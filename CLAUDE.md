# 初三知识库

## 项目目标

为初三学生（北京海淀）记录平时不会的问题、并支持后续复习的知识库。

- 解答与知识点讲解对齐人教版教材 + 北京中考题型（北京全市统一命题）
- 海淀月考/期末/自招题难度通常高于中考，题目超纲或超难度时标注「超中考」

## 目录结构

```
questions/
├── 数学/
│   ├── 学校/        # 学校课堂、作业、考试
│   ├── 课外班/      # 课外班（老刘数学等）
│   └── images/      # 数学配图，内按年月分目录（2026-09/ 等）
├── 物理/
│   ├── 学校/
│   ├── 课外班/
│   └── images/
├── 化学/
├── 英语/
├── 语文/
├── 道德与法治/
└── 其他/
```

数学、物理的第一级子目录按来源分「学校 / 课外班」，其下不再细分主题，题解文档直接放在来源目录里。归档新题时来源不清就问一句用户归哪边，别猜。

## 工作流程

两种输入方式：

1. **口头提问** — 用户描述问题（多为转述孩子的问题）
2. **拍照** — 用户把错题本/题目拍照上传。先看图片转写题目，重点核对数字、符号、单位；不清楚的地方先向用户确认，再开始解答

之后：

3. 给出详细解答（含思路、步骤、知识点）
4. 整理成 markdown 存到对应目录，**并把文件路径告诉用户**（用户后续直接阅读该 md 文档）

## 来源字段规则

能从对话上下文或照片里看出来源的（考试名、作业名、教材页码等）就照实填；**看不出就不问用户**，填「口头提问」或「错题本拍照」。

## 复习机制

- 每个归档文件含「掌握状态」和「复习记录」（见模板）
- 用户说「**抽查我**」：Claude 从 `questions/` 里出题。优先级：未掌握 > 7 天未复习 > 3 天 > 1 天。答对升级为「已掌握」；答错退回「未掌握」，并在复习记录里补一行日期和错因
- 用户说「**总结薄弱点**」：按知识点汇总题目，列出高频薄弱点，给出接下来复习建议

## 文件命名

- 题目：`YYYYMMDD-简短描述.md`（如 `20260905-二次函数顶点式.md`）
- 图片：`YYYYMMDD-NN.png`，NN 是当天题目序号（01、02…），对应那道题的第一张图，避免同一天多题、一题多图时撞号

## Markdown 文件模板

```markdown
# 问题标题

## 题目
（题目内容；如有图片用相对路径引用，统一写 `../images/YYYY-MM/文件名`，如 ![题目](../images/2026-09/20260905-01.png)）

- 来源：口头提问 / 错题本拍照 /（能确定时写考试或作业名）
- 难度：⭐ 基础 / ⭐⭐ 中考综合 / ⭐⭐⭐ 超中考
- 考查知识点：……

## 解题思路
- 关键点 1
- 关键点 2

## 解答过程
详细步骤……

## 相关知识点
- 知识点 1
- 知识点 2

## 易错点提醒
- 常见错误……

## 复盘
- 错因：粗心 / 概念不清 / 方法不熟（原本就会的写「无」）
- 掌握状态：未掌握 / 复习中 / 已掌握
- 复习记录：
  - （YYYYMMDD 抽查：变式题答对/答错，错因……）
```

## LaTeX 公式规范（GitHub 渲染保障）

GitHub 对行内公式的解析比本地预览严格，违反下列规则公式会原样显示为纯文本：

- 行内公式开头 `$` 前必须是**空白或行首**，不得紧贴中文标点（`：，、；（）「」""——`）或 `**`；紧贴时补一个半角空格，如 `即： $x=1$` 而非 `即：$x=1$`
- 结尾 `$` 后不得紧跟数字
- 公式内禁用**反斜杠+标点**（最常见是细空格 `\,`）：GitHub 会先做 markdown 转义，`\,` 变成字面逗号（坐标出现双逗号）。间隔用普通空格：写 `$K(k, m)$` 而非 `$K(k,\,m)$`。反斜杠+字母（`\dfrac`、`\left`、`\implies` 等）不受影响
- 显示公式 `$$…$$` 单独成行
- 写完自检：全文扫描每个行内公式开头 `$` 的前一个字符；在加粗标题里补空格时，`**` 后不得紧跟空格（加粗会失效），应在 `**` 后先放一个非空格字符，或把公式移出加粗

## 处理图片

- 题目图片先存到 `questions/<科目>/images/<YYYY-MM>/`（科目 = 题目所属学科；年月 = 归档当月，如 `2026-09`）
- 命名格式：`YYYYMMDD-NN.png`（`.png`/`.jpg` 按源图实际后缀）；NN 在该科目当月目录内递增
- 在 markdown 中用相对路径引用：文档与图片同科目，统一写 `../images/YYYY-MM/文件名`
- **🚨 存图铁律（防覆盖已有文件）**：
  - 取编号前先 `ls questions/<科目>/images/<YYYY-MM>/` 看已有哪些 `YYYYMMDD-*`，**递增取第一个空闲号**，绝不复用或猜测已占用的号
  - 复制用 `cp -n`（不覆盖）或先 `ls` 确认目标不存在；目标已存在就换新号，**绝不 `cp` 覆盖**
  - 原因：同一科目内序号是跨题全局递增的，不是「每道题从 01 重新数」。覆盖会毁掉别人已引用的图（曾误覆盖导致另一道题显示错图）
  - 用户直接放进仓库的截图（如 `截屏….png`、`微信图片….jpg`），先 `Read` 确认它对应本题再规范改名、移入对应科目年月目录，别凭文件名猜

## 约定

- 解答要详细，适合初三学生理解
- **只用初三（人教版 + 北京中考）学过的概念和语言**：不用高中/大学术语（物理如"电势""电势差""等电势""导通段"；数学如"导数""极限""向量"；化学如"焓""熵"等）。用大白话和直观说法（如物理里用"能不能连成一个圈""断口""正极那堆/负极那堆"代替"电势""回路""等电势段"）。类比要贴近初三生活经验，**避免引入需要额外物理/数学知识的类比**（如用"连通器原理"解释电路反而更绕，因为学生对水路不熟悉）。写完后自检：每个术语问一句"初三课本里出现过吗？"，没出现过就换掉。
- 步骤清晰，不跳步
- 标注考查的知识点
- 提醒常见错误
- 学习过程中学生的好追问（为什么这么做而不是那样做），要用通俗易懂的方式融入解答过程的对应步骤中，不要单独列「追问记录」
- 如果用图能更清楚的解释，则必须画图解释
